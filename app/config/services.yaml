# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Tests/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    GuzzleHttp\HandlerStack:
        class: GuzzleHttp\HandlerStack
        factory: ['GuzzleHttp\HandlerStack', 'create']

    GuzzleHttp\Client:
        class: GuzzleHttp\Client
        arguments:
            $config:
                handler: '@GuzzleHttp\HandlerStack'

    DigitalOceanV2\Client:
        class: DigitalOceanV2\Client
        factory: ['DigitalOceanV2\Client', 'createWithHttpClient']
        arguments:
            $httpClient: '@GuzzleHttp\Client'
        calls:
            - authenticate: ['%env(DIGITALOCEAN_API_TOKEN)%']

    DigitalOceanV2\Api\Droplet:
        class: DigitalOceanV2\Api\Droplet

    DigitalOceanV2\Api\Snapshot:
        class: DigitalOceanV2\Api\Snapshot

    SmartAssert\DigitalOceanDropletConfiguration\Factory:
        arguments:
            $defaults:
                region: '%env(DIGITALOCEAN_DROPLET_REGION)%'
                size: '%env(DIGITALOCEAN_DROPLET_SIZE)%'
                image: '%env(WORKER_MANAGER_IMAGE)%'
                tags:
                    - '%env(DIGITALOCEAN_DROPLET_TAG)%'

    App\Services\CommandExceptionRenderer:
        arguments:
            $projectDirectory: '%kernel.project_dir%'

    App\Services\InstanceCreator:
        arguments:
            $instanceName: '%env(DIGITALOCEAN_DROPLET_NAME_PREFIX)%-%env(WORKER_MANAGER_VERSION)%'

    App\Services\InstanceRepository:
        arguments:
            $dropletTag: '%env(DIGITALOCEAN_DROPLET_TAG)%'
